{{#each this}}
	<div class="col-sm-4">
		<div class="row">
			<img src="{{url}}" alt="poster img">
		</div>
		<div class="row">
			<button id="{{imdbid}}" class="add-movie-to-collection">Add</button>
		</div>
	</div>
{{/each}}